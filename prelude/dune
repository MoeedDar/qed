(rule
 (target prelude.ml)
 (deps nat.qed bool.qed eq.qed)
 (action
  (with-stdout-to %{target}
   (progn
    (echo "let text = {|")
    (cat nat.qed)
    (echo "")
    (cat bool.qed)
    (echo "")
    (cat eq.qed)
    (echo "|}")))))

(library
 (name prelude)
 (modules prelude))
